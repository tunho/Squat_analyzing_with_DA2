# 잔차 학습 기반 동작 특화 기계학습 모델을 이용한 단안 카메라 마커리스 모션 캡처의 스쿼트 각도 보정
Improving Squat Angle Accuracy in Monocular Markerless Motion Capture Using a Residual Learning-Based Exercise-Specific Machine Learning Model

## 요 약 (Abstract)
최근 단일 RGB 카메라를 이용하여 실시간으로 인체 관절을 추적하는 Google MediaPipe와 같은 마커리스(markerless) 모션 캡처 기술이 디지털 헬스케어 분야에서 널리 활용되고 있다. 그러나 이러한 단안 카메라 기반 추정 모델은 스쿼트(squat)와 같이 신체가 전후 방향으로 크게 이동하는 동작에서 원근 왜곡과 깊이 정보 손실로 인해 생체역학적 측정 오차를 유발한다. 본 연구는 이러한 기하학적 제약을 완화하기 위하여 원본 추정 각도와 정답 간의 편차를 학습하는 잔차 학습(residual learning) 기반 보정 구조와 동작 특화 전문가 모델을 제안한다. 9인 규모의 3D 데이터셋(RehabCore-9) 기반 피실험자 단위 교차 검증(person-level LOSO) 수행 결과, 제안 모델은 기존 대비 최대 42.7%의 오차 개선 효과와 평균 절대 오차(MAE) 7.57도를 달성하였다. 특히 스쿼트 카운팅의 핵심이 되는 깊은 굴곡 구간(Knee < 110°)에서는 MAE 5.41도의 고성능을 확인하였다. 본 연구는 추가 장비 없이 후처리 알고리즘만으로도 단안 카메라 기반 시스템의 신뢰성을 정밀 분석 수준으로 향상할 수 있음을 보여준다.

## 1. 서 론
비대면 디지털 헬스케어 서비스의 확산에 따라 일상생활 환경에서 사용자의 운동 자세를 자동으로 분석하는 시스템에 대한 요구가 증가하고 있다[1]. 전통적인 분석 방식은 적외선 카메라와 마커를 사용하는 광학식 장치에 의존해 왔으나, 이는 높은 비용과 통제된 환경을 필요로 한다. 이를 보완하기 위하여 최근에는 일반 카메라 영상으로부터 3차원 인체 랜드마크를 추정하는 MediaPipe 등의 기술이 대안으로 제시되고 있다.

그러나 단안 카메라 기반 3차원 관절 추정은 2차원 투영 정보만으로 소실된 깊이 정보를 복원해야 하므로 구조적 한계를 가진다. 특히 무릎 관절이 카메라 방향으로 이동하는 스쿼트 동작 시 발생하는 원근 왜곡(foreshortening)은 깊이 추정 정확도를 저하시키며, 관절 예측 시 13도 이상의 오차를 야기한다[2]. 선행 연구들은 범용 보정 모델을 통해 이를 개선하고자 하였으나, 하체 운동은 동작별로 상이한 왜곡 패턴이 존재하여 충분한 정밀도 확보에 한계가 있었다[3].

본 연구는 스쿼트의 생체역학적 궤적에 최적화된 동작 특화 전문가 모델을 제안한다. 또한 원본 추정치와 정답 간의 편차를 직접 학습하는 잔차 학습 기법을 도입하여 모델의 일반화 성능을 향상하고 실제 운동 구간의 분석 정밀도를 극대화하고자 한다.

## 2. 제안 방법
**2.1 특징 추출 및 특징 구성**
다중 적외선 카메라 기반 모션 캡처 시스템(RehabCore-9)으로부터 동기화된 3D 데이터를 활용하였다. MediaPipe를 통해 추출한 하체 3차원 랜드마크 좌표에 더해, 원근 변화에 강건한 특징을 구성하기 위해 다리 분절의 픽셀 비율(thigh/shank ratio)과 각 관절의 가시성 점수(visibility score)를 전역 특징으로 융합하였다.

**2.2 잔차 학습 및 전문가 모델 구축**
정답 절대 각도를 직접 예측하는 대신, Residual = 정답 각도 - 추정 각도를 학습 목표 변수로 설정하였다. 모델은 원근 왜곡에 의해 발생하는 비선형 오차 패턴을 앙상블 트리 계열인 ExtraTrees 회귀 모델을 통해 학습하였다. 또한 실험 조건별 왜곡 특성을 고립시키기 위해 스쿼트 고유 패턴만을 집중적으로 학습하는 동작 특화 전문가 모델 아키텍처를 채택하였다.

## 3. 실험 결과
**3.1 검증 환경 및 분석 방법**
피실험자 단위 교차 검증(Person-level LOSO) 방식을 통해 9인 전체에 대한 일반화 성능을 평가하였다. 특히 본 연구에서는 보점 모델의 실용적 효용성을 검증하기 위해, 전체 구간 성능뿐만 아니라 실제 운동 판별이 이루어지는 핵심 동작 구간(Knee < 110°)에 대한 분석을 병행하였다.

**3.2 정량적 분석 및 성능 비교**
실험 결과, 제안 모델은 기존 방식의 평균 오차인 13.22도를 7.57도로 낮추며 전 구간에서 42.7%의 오차 개선 효과를 보였다. 주목할 점은 스쿼트 카운팅이 결정되는 핵심 동작 구간(<110°)에서의 보정 성능이다. 해당 구간에서 기존 방식은 오차가 18.96도까지 급격히 증가하며 기능을 상실했으나, 제안 모델은 MAE 5.41도의 높은 정밀도를 유지하였다. 이는 중요 동작 구간에서 오차의 71.5%를 성공적으로 제거했음을 입증한다.

**3.3 정성적 및 시계열 분석**
시계열 궤적 분석에서도 제안 모델은 효과를 입증하였다. 하강 굴곡의 정점(Peak) 구간에서 원본 데이터는 깊이 소실로 인해 각도 요동(jittering) 현상이 심각하게 발생하였으나, 제안 모델은 정답 데이터의 궤적을 안정적으로 추종하며 매끄러운 동작 선형성을 복원하였다(그림 1 참조). 시점별 분석 결과 측면 대비 정면의 오차는 9.30도로 다소 높게 나타났으나, 이 역시 원본 오차(13.12도)를 약 30% 개선한 수치이다.

## 4. 결 론
본 연구는 단안 카메라 기반 마커리스 시스템의 구조적 한계를 잔차 학습과 동작 특화 설계를 통해 개선하였다. 피실험자 전수에 대한 교차 검증을 통해 40% 이상의 전체 오차 개선을 확인하였으며, 특히 운동 카운팅 핵심 구간에서 5도대의 정밀도를 확보하였다는 시사점이 크다. 향후 시계열 연속성을 고려한 딥러닝 기법과의 융합을 통해 분석 단계를 더욱 고도화할 계획이다.

## 참 고 문 헌
[1] C. Lugaresi et al., "MediaPipe: A Framework for Building Perception Pipelines," arXiv:1906.08172, 2019.
[2] L. Wang et al., "Markerless Motion Capture Techniques in Sports and Rehabilitation," J. Biomech, 2021.
[3] J. Martinez et al., "A simple yet effective baseline for 3d human pose estimation," ICCV, 2017.
